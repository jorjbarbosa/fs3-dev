/**
 * @format
 */

import { AppRegistry } from "react-native";
import Login from "./src/screens/credentials";
import { name as appName } from "./app.json";
AppRegistry.registerComponent(appName, () => Login);
